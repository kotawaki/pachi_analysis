# パチンコ伝播分析プロジェクト

## このプロジェクトの目的
パチンコのスランプグラフ由来CSVから「グループ内の当たり伝播ペア」を統計的に抽出する。
「台Aが当たると30分以内に台Bが当たりやすい」という相関を、3ヶ月分のデータで検証する。

最終ゴール: 全377台を分析し、最終的に39〜77番の39台に絞ってパターンが成立するか検証。

## 重要な前提(これは確定事項なので議論しない)
- **当否そのものは予測不能**。パチンコの当たり乱数は台ごと独立。「次に当たる台の予測」は目標にしない。
- **狙うのは伝播の相関構造**(リフト値ベース)であって、未来予知ではない。
- **9グループ間の引っ張り合いは検出されない**(2日分でlift≒1.0=無相関を確認済み)。分析はグループ内に絞る。

## データ構造
CSV: `csv/analyze/YYYYMMDD/YYYYMMDD_analyze.csv`
カラム: Date, Machine, Group, Island, 種別, 開始時刻, 開始差玉, 終了時刻, 終了差玉, 増減差玉, 時間(分)
- Machine: "001"〜"377" (ゼロパディング3桁)
- Group: "1"〜"9" (店内の論理グループ。機種ではなく位置パターン)
- Island: "s1"〜"s20" (物理島)
- 種別: "当り"/"大当り"/"稼働なし(NS)"/"稼働なし(NC1)"/"稼働なし(NE)" など
  → **「稼働なし」を含む文字列をidle判定する**(NS/NC/NEの完全一致ではない点に注意)

## ファイル構成と役割
- `build_snapshots.py`: CSV → 10分刻みスナップショットJSON (10:00-22:30, 76step)
- `propagation.py`: 伝播ペア抽出エンジン(リフト計算の本体)
- `daily_ingest.py`: 日次取り込み。新CSVを検出→snapshot化→履歴更新→レポート生成
- `pair_history.json`: ペアごとの日次lift履歴(蓄積される)
- `reports/YYYYMMDD_report.md`: 日次レポート

## 主要コマンド
```bash
python daily_ingest.py                    # 未取込の全日を処理(通常はこれだけ)
python daily_ingest.py --rebuild          # 履歴を破棄して全再構築
python daily_ingest.py --summary          # 取込せずサマリ表示
python propagation.py --window 3 --min-count 3   # 単発の伝播分析
```

## 指標の定義(変更しないこと)
- リフト値 = P(window内にB点火 | 直前にA点火) / P(Bがランダムwindowで点火)
- 再現性スコア = (lift>=1.5だった日数) / (観測日数)
- mean_lift = count加重平均

## 統計の作法(厳守)
- count < 10 のペアは信用しない(2日分の今は件数不足で当然)
- 「lift高 かつ count多 かつ 再現性高」の3条件が揃って初めて候補
- バックテストは学習期間と検証期間を必ず分ける(チェリーピッキング防止)
- 39-77番に絞る根拠は、検証期間のデータを見てから決めてはいけない(後付け禁止)

## コーディング規約
- Python標準ライブラリのみで完結させる(pandas等の外部依存を増やさない)
- 日本語コメント・出力でOK
- 既存スクリプトの指標定義を勝手に変えない。変える時は必ず理由を説明してから

## 作業スタイル
- 新しいデータが来たらまず daily_ingest.py を回し、結果の解釈を添える
- 統計的に弱い結果を「強い」と言わない。件数不足なら正直にそう言う
- 派手な結論より再現性を重視
